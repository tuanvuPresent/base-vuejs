version: 2.1

orbs:
  node: circleci/node@4.7

jobs:
  build-and-test:
    docker:
      - image: cimg/node:16.10
    steps:
      - add_ssh_keys:
          fingerprints:
            - "96:76:4d:a9:9c:06:9e:98:a3:77:60:7f:4a:31:d8:84"
      - checkout
      - run:
          name: Create branch
          command: |
            git config user.email "tuanvubk96@gmail.com"
            git config user.name "circleci"
            git checkout -B `git symbolic-ref --short HEAD`-build
      - node/install-packages:
          pkg-manager: npm
      - run:
          name: Run install
          command: npm install
      - run:
          name: Run tests
          command: npm run build
      - run:
          name: Git push
          command: |
            git add --all
            git commit -m 'auto build app'
            git push -f origin HEAD
workflows:
  sample:
    jobs:
      - build-and-test
